# Core CLI / extraction stack
PyPDF2>=3.0.0
pypdf>=4.0.0
pandas>=2.0.0
openpyxl>=3.1.0
tqdm>=4.66.0
pdfplumber>=0.11.0
camelot-py[cv]>=0.11.0
rapidfuzz>=3.0.0

# Optional AI-based extraction (install with
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# to fetch the CPU wheels from the PyTorch index)
dedoc==2.6
torch==2.5.1
torchvision==0.20.1
transformers==4.45.0
timm==0.9.16
pdf2image==1.10.0
# openai>=1.0.0  # Habilita o comando pdf2jsonllm para chamar modelos compatíveis
